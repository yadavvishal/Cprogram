#include<stdio.h>
#include<string.h>
int main()
{
	int length,i;
	char a[100];
	printf("Enter the string:\n ");
	fgets(a, sizeof(a), stdin);
	for(i=0;i<strlen(string);i++)
	printf("%d",string(i));
	length=strlen(a);
	printf("length of string are=%d",length);
	return 0;
}
