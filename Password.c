#include<stdio.h>
int main()
{
    char pass[100];
    int flag_spec=0,flag_digit=0,,length,alpha=0,i;
    printf("Enter your password");
    scanf("%[^\n]",pass);
    length=strlen(pass);
    if(length<8)
    {
        printf("Wrong password:");
    }
    else
    {
        for(i=0;pass[i];i++)
        {
                  ck cjkcb 
        }
    }

}